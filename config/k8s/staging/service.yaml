# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: lkrm-staging
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: lkrm-staging
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80 # Pod 里实际监听的 8000
